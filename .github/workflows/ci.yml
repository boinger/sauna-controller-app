name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    # macos-15 runners ship Xcode 16.x with iPhone 16 simulators;
    # local dev uses iPhone 17 Pro (Xcode 17 / macOS 26)
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer

      - name: Build
        run: |
          xcodebuild build \
            -project SaunaController/SaunaController.xcodeproj \
            -scheme SaunaController \
            -destination 'platform=iOS Simulator,name=iPhone 16 Pro' \
            -quiet

      - name: Test
        run: |
          xcodebuild test \
            -project SaunaController/SaunaController.xcodeproj \
            -scheme SaunaController \
            -destination 'platform=iOS Simulator,name=iPhone 16 Pro' \
            -only-testing:SaunaControllerTests \
            -quiet
